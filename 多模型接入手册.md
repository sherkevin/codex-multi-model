# Codex 桌面端多模型接入手册（通用版）

> **这份手册解决什么问题**：让 Codex 桌面端的 `/model` 面板里同时出现多个厂商的模型，各自用各自的密钥，可以在不同窗口里并行使用——包括那些只提供旧式 `chat/completions` 接口、原本根本接不进 Codex 的模型。
>
> **验证环境**：macOS + codex-cli 0.142.5 + Codex 桌面端（ChatGPT.app）。其他平台见附录 B（未验证）。
>
> **本手册描述机制与字段，不含程序代码**。代理程序本体是同目录的 `codex-model-router.py`，目录生成器是 `regen-model-catalog.py`；第 5 节逐项说明你需要在这两个文件里改哪些地方。

---

## 0. 开工前：你需要具备什么

**小结：三个前提，缺一个都走不通。**

| 前提 | 怎么确认 | 不满足怎么办 |
|---|---|---|
| macOS，且已装 Codex 桌面端 | 能打开应用并正常对话 | 本手册不适用 |
| 系统自带 Python 3 | `python3 --version` 有输出 | 装任意 Python 3.9+ |
| 至少两个上游的可用密钥 | 各自能用 `curl` 打通 | 只有一个上游就不需要本方案 |

再确认一下 codex 的配置目录（后文统一写作 `$CODEX_HOME`，默认就是 `~/.codex`）：

```
ls ~/.codex/config.toml        # 存在即说明 $CODEX_HOME = ~/.codex
codex --version                # 记下版本号，0.142.5 之后的行为可能变
```

若你设置过 `CODEX_HOME` 环境变量指向别处，后文所有 `~/.codex` 都替换成那个路径。

---

## 1. 速读层（30 秒）

**一句话结论**：Codex 原生只允许「一个提供商 + 一个模型名」，所以要在本机起一个**回环代理**冒充那个唯一的提供商，由它按模型名把请求分发到不同上游、换上各自的密钥；`/model` 面板里显示什么，则由一份**自定义模型目录**单独控制。

你要动四个文件，各管一件事，缺一不可：

| 文件 | 管什么 | 改完是否要重启桌面端 |
|---|---|---|
| `~/.codex/config.toml` | 指定「唯一提供商 = 代理」、默认模型、默认思考档 | 否 |
| `~/.codex/codex-model-router.py` | 按模型名分发上游 + 协议翻译 | 否（重启代理即生效） |
| `~/.codex/custom-model-catalog.json` | `/model` 面板显示哪些模型、叫什么名字、有哪些思考档 | **是** |
| `~/Library/LaunchAgents/com.<你的标识>.codex-model-router.plist` | 让代理开机常驻、崩溃自拉 | 否 |

三条日常命令（把 `<LABEL>` 换成你自己的任务标识，`<PORT>` 换成你选的端口）：

```
launchctl kickstart -k gui/$(id -u)/<LABEL>                              # 重启代理
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:<PORT>/v1/models   # 健康检查，期望 200
tail -f /tmp/codex-model-router.log                                      # 看路由与用量
```

---

## 2. 读前必备概念

每个术语一句话大白话，后文不再解释。

| 术语 | 一句话解释 |
|---|---|
| **提供商（provider）** | 一组「服务器地址 + 用哪个环境变量取密钥 + 说哪种协议」的配置。Codex 一次请求只用一个。 |
| **Responses API** | OpenAI 较新的那套接口，请求体里是 `input` 数组、回复是命名事件流。codex 0.142.5 **只会说这一种**。 |
| **chat/completions** | 传统那套接口，请求体里是 `messages` 数组。很多第三方网关只提供这一种。 |
| **直通（passthrough）** | 上游本来就会说 Responses，代理原样转发，不改内容。 |
| **翻译（translate）** | 上游只会说 chat，代理把请求从 Responses 改写成 chat、把回复从 chat 改写回 Responses。 |
| **密钥 / AK** | 访问上游的凭证。本方案中真实密钥只存在环境变量里，任何配置文件都不落明文。 |
| **模型目录（catalog）** | 一份 JSON，只决定 `/model` 面板里列什么、每项叫什么名字、有哪些思考档。**它不含任何上游地址或密钥。** |
| **思考档（reasoning effort）** | `low / medium / high / xhigh` 之类的档位，表示让模型多花多少推理预算。 |
| **回环（loopback）** | `127.0.0.1`，只有本机能连的地址。代理监听在这里，不对外暴露。 |
| **launchd / LaunchAgent** | macOS 的常驻进程管理器；plist 是它的任务描述文件。 |
| **会话日志（rollout）** | Codex 给每个会话落的一份 JSONL，位于 `~/.codex/sessions/<年>/<月>/<日>/`。排错的第一手证据。 |

---

## 3. 填空表：先把这七个值定下来

**小结：整套配置只有七个因人而异的量，先定好，后面就是照着填。**

| 记号 | 含义 | 怎么定 | 举例 |
|---|---|---|---|
| `<PORT>` | 代理监听端口 | 任选一个空闲高位端口，用下方命令确认没被占 | `18900` |
| `<LABEL>` | launchd 任务标识 | 惯例是反写域名，**必须全局唯一** | `com.zhangsan.codex-model-router` |
| `<UPSTREAM_i>` | 第 i 个上游的接口根地址 | 查该厂商文档，通常以 `/v1` 结尾 | `https://api.example.com/v1` |
| `<ENVVAR_i>` | 第 i 个上游密钥的环境变量名 | 自己起，见名知意 | `EXAMPLE_API_KEY` |
| `<MODE_i>` | 第 i 个上游是直通还是翻译 | 按第 4 节的判定流程实测决定 | `translate` |
| `<SLUG_i>` | 模型在请求里的真名 | **必须用上游认的那个字符串**，不能自己改 | `vendor-a/chat-large` |
| `<DISPLAY_i>` | 面板上显示的名字 | 随便起，建议带上来源 | `Chat-Large (公司网关)` |

确认端口空闲：

```
lsof -nP -iTCP:<PORT> -sTCP:LISTEN        # 无输出 = 空闲
```

**`<SLUG_i>` 是整套方案的连接点**，会同时出现在三个地方：代理的路由表、模型目录的 `slug` 字段、`config.toml` 的 `model` 字段。三处必须逐字符一致，错一个字就是「面板上能选、一发就报错」。

---

## 4. 第一节 为什么必须加一层代理

**小结：因为 `/model` 面板只能改「模型名」这一个自由度，「去哪个服务器、用哪把钥匙」是另一个自由度，面板碰不到。**

Codex 每次发出的上游请求，可以写成一个三元组：

$$
R \;=\; \bigl\langle\, p.\texttt{base\_url},\;\; p.\texttt{auth},\;\; m \,\bigr\rangle,
\qquad p \;=\; \texttt{config.model\_provider}
$$

其中：

- $R$ ——一次真实发往上游的 HTTP 请求；
- $p$ ——当前生效的提供商，**由 `config.toml` 里的 `model_provider` 单值决定**，全局唯一；
- $p.\texttt{base\_url}$ ——请求发往哪个服务器，由 $p$ 决定，与模型名无关；
- $p.\texttt{auth}$ ——用哪把密钥，同样由 $p$ 决定，与模型名无关；
- $m$ ——模型名字符串，**这是 `/model` 面板唯一能改的量**。

关键推论：在 `/model` 里从 A 切到 B，只是把 $m$ 从 `"A"` 换成 `"B"`，而 $p$ 一动不动。若 A、B 本属不同厂商、不同密钥，切换后请求仍会打到原厂商、带着原密钥，结果是 404 或 401。

**所以唯一可行的解法**：把 $p$ 固定成一个本机代理，让「按 $m$ 选服务器和密钥」这件事发生在代理内部——

$$
p \;\equiv\; \texttt{router}\ \bigl(127.0.0.1{:}\texttt{<PORT>}\bigr)
\qquad\Longrightarrow\qquad
R' \;=\; \bigl\langle\, r(m).\texttt{base},\;\; r(m).\texttt{key},\;\; m \,\bigr\rangle
$$

其中 $r(\cdot)$ 是代理内的路由函数，把模型名映射到真正的上游。此时 $R'$ 的每一项都随 $m$ 变化，多模型自由切换才成立。

还有一条硬约束叠加在上面：**codex 0.142.5 已移除 `wire_api = "chat"`**。只提供 chat/completions 的网关，就算你给它单配一个提供商也接不进来。代理的第二个职责——协议翻译——就是为这类上游存在的。

---

## 5. 第二节 整体链路

**小结：桌面端只认识回环上的代理；代理才认识真正的上游。**

```
桌面端窗口 A ─┐                                       ┌─→ 上游 1（原生 Responses，直通）
桌面端窗口 B ─┼─→ 127.0.0.1:<PORT> ──按模型名分发──┼─→ 上游 2（原生 Responses，直通）
桌面端窗口 C ─┘        （唯一提供商）                  └─→ 上游 3（仅 chat，需翻译）
                              │
                       密钥取自环境变量
```

三个事实值得单独记住：

1. **模型是会话级的，不是全局的。** 模型名记在每个会话每一轮的 `turn_context` 里；`config.toml` 里的 `model =` 只是**新建会话的默认值**。在窗口 A 切模型不会影响已经开着的窗口 B——所以「一个窗口跑模型甲、另一个跑模型乙」是支持的。
2. **代理是多线程的**，每个请求一个线程，多窗口同时出流不会互相排队。
3. **共享上游 = 共享限流。** 若多个模型走同一个网关同一把密钥，多窗口齐跑是叠加在同一份配额上；用独立密钥的上游不受影响。

---

## 6. 第三节 判定每个上游该用直通还是翻译

**小结：直接拿一个最小请求去打上游的 `/responses`，能出流就是直通，报不支持就是翻译。**

判定规则：

$$
\texttt{mode}(u)=
\begin{cases}
\texttt{passthrough}, & u/\texttt{responses}\ \text{返回 200 且是事件流}\\[2pt]
\texttt{translate}, & u/\texttt{responses}\ \text{返回 404 / 400（不支持该端点）}\\[2pt]
\text{待查}, & \text{返回 401 / 403}
\end{cases}
$$

其中 $u$ 是该上游的接口根地址（`<UPSTREAM_i>`）。返回 401/403 说明**密钥或权限有问题，还没走到协议判定这一步**，先把认证弄通再判。

探测方法：向 `<UPSTREAM_i>/responses` 发一个最小请求体（只含模型名、一条用户消息、开启流式），带上 `Authorization: Bearer <你的密钥>`。同一个上游对不同模型的支持度可能不同——**要逐个模型探**，不能只探一个就推广到整个网关。

实践中常见的情形：同一个企业网关下，某些模型走了官方 Responses 通道，另一些只暴露 chat/completions。这种混合情况正是本方案要同时支持两种模式的原因。

---

## 7. 第四节 分步实施

### 7.1 第一步：备好凭证，且不落盘

**小结：真实密钥只写进 shell 启动文件的环境变量，配置文件里一律只出现变量名。**

为每个上游在 `~/.zshrc`（bash 用户是 `~/.bashrc`）里导出一个变量，变量名就是你在填空表里定的 `<ENVVAR_i>`。后续所有配置文件引用的都是**变量名**，不是值。

为什么必须这样：`config.toml` 会被备份、会被同步、出错时会被贴给别人看；密钥一旦写进去就等于永久泄露风险。这条是本方案的不可协商项。

改完后开一个新终端确认变量已生效（用 `echo ${#<ENVVAR_i>}` 看长度，避免把密钥打到屏幕上）。

### 7.2 第二步：让 Codex 只认一个提供商

**小结：在 `config.toml` 里声明代理为提供商，并把 `model_provider` 指向它。**

新增一段提供商定义，字段含义：

| 字段 | 填什么 | 为什么 |
|---|---|---|
| `name` | 任意显示名，如 `Local Router` | 仅用于界面显示 |
| `base_url` | `http://127.0.0.1:<PORT>/v1` | 代理监听地址。只走回环，不对外 |
| `experimental_bearer_token` | 任意占位串 | 回环通信不需要真凭证；**这不是密钥**，代理不校验它 |
| `wire_api` | `responses` | 代理对 Codex 一侧始终说 Responses |

顶层字段：

| 字段 | 填什么 | 备注 |
|---|---|---|
| `model_provider` | 上面那段提供商的名字 | **这一行是整套方案的枢纽** |
| `model` | 某个 `<SLUG_i>` | 新建会话的默认模型；桌面端切模型时会回写此行 |
| `model_reasoning_effort` | 如 `medium` | 新建会话的默认思考档；同样会被回写 |
| `review_model` | 某个 `<SLUG_i>` | 代码评审用的模型。**留空会回落到内置模型而打不通** |
| `model_catalog_json` | 目录文件的**绝对路径** | 见 7.5 |

**可选：直连备份提供商。** 可以额外为每个上游各留一段提供商定义（`base_url` 填 `<UPSTREAM_i>`、`env_key` 填 `<ENVVAR_i>`、`wire_api = "responses"`），供代理挂掉时用命令行 `--profile` 直连应急。注意**桌面端不读 `--profile`**，这条只对命令行有效，且只对直通类上游有意义。

### 7.3 第三步：部署代理并理解它的职责边界

**小结：把 `codex-model-router.py` 放进 `~/.codex/`，改掉里面的路由表和端口；它只做两件事——按名分发、协议翻译。**

**职责一：路由。** 路由函数定义为

$$
r(m)=
\begin{cases}
\texttt{ROUTES}[m], & m \in \operatorname{dom}(\texttt{ROUTES})\\[2pt]
\texttt{FALLBACK\_ROUTE}, & \exists\, q \in P:\; m \text{ 以 } q \text{ 开头}\\[2pt]
\bot\ (\text{显式报错}), & \text{其他}
\end{cases}
$$

其中：

- $m$ ——请求体里的模型名；
- $\texttt{ROUTES}$ ——显式登记表，每项给出「上游地址、取哪个环境变量当密钥、直通还是翻译」，即你填空表里的 `<UPSTREAM_i>` / `<ENVVAR_i>` / `<MODE_i>`；
- $P$ ——按前缀兜底的模型名前缀集合（脚本里预置了 OpenAI 官方模型的前缀）；
- $\bot$ ——**返回明确错误，绝不静默回落到别的模型**。

最后一条是设计要点：静默回落会让你以为在用 A、实际是 B 在回答，比直接失败危险得多。

**职责二：翻译。** 仅对 `translate` 模式的模型生效，两个方向：

- 请求方向：Responses 的 `input` 数组 → chat 的 `messages` 数组。`developer` 角色降级成 `system`（chat 协议不认这个角色）；`reasoning` 条目直接丢弃（上游不接受回传推理内容）。
- 回复方向：chat 的流式增量 → Responses 的命名事件流。每个事件必须同时有 `event:` 行与 `data:` 行，`sequence_number` 全局单调递增。

**用量字段必须补齐。** 上游用 `prompt_tokens / completion_tokens`，Responses 要 `input_tokens / output_tokens`；上游若整段不给用量，也必须填 0 而非省略——否则 Codex 会以 `failed to parse ResponseCompleted` 硬失败。

部署后先手动前台跑一次，确认启动横幅里每个模型后面都带着密钥变量名而不是「缺少」，再进行下一步。

### 7.4 第四步：常驻化

**小结：用 LaunchAgent 保证开机自起、崩溃自拉，并且一定要显式加载你的 shell 启动文件。**

在 `~/Library/LaunchAgents/<LABEL>.plist` 写一个任务，关键字段：

| 字段 | 填什么 | 作用 |
|---|---|---|
| `Label` | `<LABEL>` | 任务标识，`launchctl` 用它。必须与文件名一致 |
| `RunAtLoad` | 真 | 登录即启动 |
| `KeepAlive` | 真 | 进程退出后自动重启 |
| `ProgramArguments` | 用 shell 包一层：先 source 启动文件，再 exec 解释器执行代理脚本 | 见下方说明 |
| `StandardOutPath` / `StandardErrorPath` | 同一个日志路径，如 `/tmp/codex-model-router.log` | 路由与用量日志 |

**为什么必须显式 source 启动文件**：zsh 只在**交互式**启动时读 `~/.zshrc`，而 launchd 启的是非交互进程。不显式 source，代理就拿不到密钥环境变量，症状是所有模型都报「缺少密钥」。这是本方案最高频的坑。

装载与验证：

```
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/<LABEL>.plist   # 首次装载
launchctl kickstart -k gui/$(id -u)/<LABEL>                             # 之后每次改动都用它重启
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:<PORT>/v1/models
```

**重启前先确认没有正在进行的请求**：`lsof -nP -iTCP:<PORT> -sTCP:ESTABLISHED`，有输出就等它跑完，否则会打断正在出流的会话。

### 7.5 第五步：让 `/model` 面板出现你的模型

**小结：`model_catalog_json` 是【整体替换】内置目录，不是合并——这是第二高频的坑。**

$$
C_{\text{final}} \;=\; C_{\text{custom}}
\qquad\bigl(\text{不是 } C_{\text{builtin}} \cup C_{\text{custom}}\bigr)
$$

其中：

- $C_{\text{final}}$ ——`/model` 面板实际展示的集合；
- $C_{\text{custom}}$ ——你那份 JSON 文件里的全部条目；
- $C_{\text{builtin}}$ ——Codex 二进制自带的目录。

后果：JSON 里没写的内置模型会**从面板上消失**。所以正确做法不是手写这份文件，而是用同目录的 `regen-model-catalog.py` 生成——它先用一个临时配置目录向 Codex 二进制问出当前版本的完整内置目录（临时目录是必需的，否则你自己的目录会把它挡住），原样保留，再把你的自定义条目追加进去。

控制显示效果的字段：

| 字段 | 作用 | 要点 |
|---|---|---|
| `slug` | 模型真名 | 必须与路由表的键**完全一致**。这是两边唯一的连接点 |
| `display_name` | 面板上显示的名字 | 即 `<DISPLAY_i>`，建议带上来源 |
| `description` | 名字下方的小字 | 建议写上游名，一眼看出走哪家 |
| `visibility` | `list` 显示 / `hide` 隐藏 | `hide` 只从面板移除，子系统仍可按 slug 调用 |
| `priority` | 排序权重 | 自定义条目给 0 即可 |
| `context_window` / `max_context_window` | 上下文上限 | 拿不准就保守填，确认后再上调 |
| `supported_in_api` | 是否可被 API 调用 | 自定义条目须为真 |
| `base_instructions` | 该模型的系统提示词底稿 | 会作为 `instructions` 发给上游 |

**目录不携带提供商信息**：它只管界面。哪个 slug 去哪个上游完全由代理路由表决定。

改完目录**必须重启桌面端**才生效——这是四个文件里唯一需要重启的。

### 7.6 第六步：思考档位

**小结：档位由「目录（每个模型允许哪些档）」和「桌面端配置（界面上摆出哪些档）」两处共同决定，取交集。**

| 位置 | 字段 | 管什么 |
|---|---|---|
| 目录中每个模型条目 | `supported_reasoning_levels` | 该模型合法的档位数组，每项含 `effort` 与一句说明文字 |
| 目录中每个模型条目 | `default_reasoning_level` | 该模型的默认档 |
| `config.toml` 的 `[desktop]` 段 | `enabled-reasoning-efforts` | 界面上摆出哪些档的按钮（全局，不分模型） |
| `config.toml` 顶层 | `model_reasoning_effort` | 新建会话的默认档（会被桌面端回写） |

两处不一致时的表现：`[desktop]` 里多出来的档在界面上点得到，但不在目录声明的合法集合内——要真正启用，得先把它们加进对应模型的 `supported_reasoning_levels`。

**必须知道的诚实限制**：档位**只对直通模型真正生效**。直通模式下代理原样转发整个请求体，`reasoning` 字段随之抵达上游；翻译模式下代理构造的 chat 请求体只含模型名、消息、是否流式、输出上限四项，**不含任何推理档字段**。所以给翻译类模型调档位，界面会变、上游收不到，属于装饰性操作。若你的上游有自己的推理参数（各家命名不同），需要在翻译层显式映射才能生效。

### 7.7 第七步：跑一遍验证清单

见第 9 节。**不要跳过工具调用那一项。**

---

## 8. 第五节 移植清单：你必须自己改的地方

**小结：两个脚本里只有五处是因人而异的，其余不用动。**

`codex-model-router.py` 中：

| 位置 | 改成什么 |
|---|---|
| 监听端口常量 | 你的 `<PORT>` |
| 上游地址常量 | 你的每个 `<UPSTREAM_i>` |
| `ROUTES` 路由表 | 每个模型一行：`<SLUG_i>` → 上游地址、`<ENVVAR_i>`、`<MODE_i>` |
| 前缀兜底路由 | 若你不用 OpenAI 官方模型，可整段删掉 |
| 输出上限 | 脚本里固定为 8192。上游支持更长输出时按需上调 |

`regen-model-catalog.py` 中：

| 位置 | 改成什么 |
|---|---|
| `CUSTOM` 列表 | 每个模型一项：`<SLUG_i>`、`<DISPLAY_i>`、来源描述、上下文上限 |
| 目录输出路径 | 与 `config.toml` 里 `model_catalog_json` 保持一致 |

`ROUTES` 的键与 `CUSTOM` 的 slug **必须完全对应**：目录里有、路由表里没有 → 面板能选但一发就报错；路由表里有、目录里没有 → 能用但面板上看不见（可作为隐藏模型）。

---

## 9. 第六节 验证清单

**小结：每一层都要单独证明它成了，不能只看「界面没报错」。**

| 层 | 怎么验 | 通过标准 |
|---|---|---|
| 代理活着 | `curl` 打 `/v1/models` | HTTP 200 |
| 密钥到位 | 看日志启动横幅 | 每个模型后面带着密钥变量名，而非「缺少」 |
| 路由正确 | 给每个模型各发一句「你是哪个模型系列」 | 自报身份与所选模型一致 |
| 翻译正确 | 让每个翻译类模型**真的执行一次工具调用** | 事件流出现 `response.completed` 且无 error |
| 并发正确 | 同时对两个模型发流，记录首末增量时刻 | 两条流的时间窗有重叠 |
| 用量正确 | 看日志的用量行 | 上游用量非空，映射后三个数字自洽 |
| 面板正确 | 打开 `/model` | 自定义模型都在，且没有内置模型意外消失 |

**倒数第四条要强调**：只让模型回一句文本，不足以证明翻译层是对的。工具调用才会触发消息序列的结构约束，而那正是最容易出错的地方。**每个翻译类模型都要单独验一次**，不能只验一个就推广。

---

## 10. 第七节 协议翻译必须守住的三条不变式

**小结：违反任意一条，都会被上游整段拒绝或让 Codex 解析失败。**

**不变式一（工具调用配对）**：对每一条带 `tool_calls` 的 assistant 消息 $A$，其**紧随其后**必须是恰好 $|A.\texttt{tool\_calls}|$ 条 tool 消息，且 id 逐一对应：

$$
\forall A:\quad \texttt{msgs}[i+1 \,..\, i+|A.\texttt{tool\_calls}|]\ \text{均为 tool 消息，且 id 序列}\;=\;\bigl(\,\texttt{tc}.id\,\bigr)_{\texttt{tc}\,\in\,A.\texttt{tool\_calls}}
$$

其中 $i$ 是 $A$ 的下标。两种典型违反：

- **并行调用被拆散**：Responses 把并行工具调用表示为**连续多个** `function_call` 条目。若逐个包成独立 assistant 消息，序列会变成 `assistant[调用0] → assistant[调用1] → tool0 → tool1`，第一条后面跟的是 assistant 而非它的 tool 回复，上游报错并点名第一个调用的 id。**正确做法：连续的 `function_call` 合并进同一条 assistant 消息的 `tool_calls` 数组。**
- **孤儿调用**：会话被打断（模型已发出调用、输出还没回来就按了取消），历史里留下没有输出的调用。**正确做法：给它补一条占位输出**，否则整个请求被拒。

**不变式二（用量字段完备）**：`response.completed` 事件里的用量对象必须五项齐全（输入、输出、合计、缓存明细、推理明细），缺失即导致 Codex 硬失败。上游不给就填 0。

**不变式三（事件流格式）**：每个事件同时具备 `event:` 与 `data:` 两行，`sequence_number` 全局单调递增，且流必须以 `response.completed` 收尾。

---

## 11. 第八节 排错手册

**小结：先看代理日志定位是哪一层，再去会话日志看真实数据。**

| 症状 | 根因 | 处置 |
|---|---|---|
| 上游 400：`tool_calls must be followed by tool messages … <某 id> did not have response messages` | 违反不变式一 | 检查连续 `function_call` 是否被合并；检查孤儿调用是否补了占位 |
| 日志「缺少某密钥」 | launchd 没读到环境变量，或该上游未配密钥 | 确认 plist 里显式 source 了 shell 启动文件；确认变量已导出 |
| Codex 报 `failed to parse ResponseCompleted` | 违反不变式二 | 补齐用量五项，缺的填 0 |
| `/model` 里内置模型集体消失 | 目录是替换语义 | 用生成脚本重生成，别手写 |
| 面板能选、一发就 404 / 401 | 目录 slug 与路由表键不一致，或该 slug 没有可用密钥 | 两边对齐 slug；无密钥的条目设 `visibility: hide` |
| 换了模型但回答风格没变 | 目录改了没重启桌面端 | 重启桌面端 |
| 健康检查连不上 | 代理没起来，或端口被别的进程占了 | 看日志尾部；用 `lsof` 查端口占用 |
| 「用量」数字远小于预期 | **不是 bug**，见下 | 不必处理 |

**关于用量数字**：Codex 界面显示的「tokens used」并非上游返回的总量，而是

$$
\texttt{displayed} \;=\; \texttt{total\_tokens} \;-\; \texttt{cached\_input\_tokens}
$$

其中 $\texttt{cached\_input\_tokens}$ 是上游命中前缀缓存、**未重新计费**的那部分输入。所以同一段长上下文，缓存命中率高的模型显示值会显著偏小。这是显示口径差异，不是漏算——不要据此判断某条路径「少算了输入」。

**证据的取用顺序**：

1. 代理日志 ——看走了哪个模型、哪种模式、工具数、用量。
2. `~/.codex/sessions/<年>/<月>/<日>/rollout-*.jsonl` ——看每轮真实的条目序列与 `turn_context.model`。判断并行调用、孤儿调用只能靠它。
3. **拿失败会话的历史重放给代理** ——这是唯一能证明「修好了」的方式。改完翻译逻辑后，用出问题那次的真实历史再打一遍，比造个新例子可靠得多。

---

## 12. 第九节 升级 Codex 之后必做

**小结：升级会带来新的内置模型，而你的目录是替换语义，不重生成就看不到它们。**

1. 重新生成目录：运行 `regen-model-catalog.py`。
2. 重启桌面端。
3. 复查协议支持是否有变（0.142.5 移除了 `wire_api = "chat"`，后续版本可能再变）。
4. 跑一遍第 9 节验证清单，尤其是**工具调用**那一项——协议细节最容易在升级中变化。

注意生成脚本会**原样保留内置条目的 `visibility`**。也就是说，你手工隐藏过的内置模型在重生成后会重新出现，需要再隐藏一次。

---

## 13. 第十节 已知限制

**小结：五条，都是设计取舍而非缺陷，但用之前必须知道。**

1. **思考档位对翻译类模型无效。** 界面能调，上游收不到（7.6 节）。
2. **同上游共享限流。** 共用一把密钥的模型，多窗口并发是叠加在同一份配额上。
3. **直通模式会转发未知顶层字段。** 诸如服务等级之类的字段会原样送到上游；换上游时如遇莫名 400，先怀疑这里。
4. **没有密钥的目录条目是死条目。** 选中即失败。代理会返回明确错误（不静默替换模型），但更好的办法是把它们 `visibility: hide` 掉。
5. **代理是单点。** 它挂了所有模型都不可用。`KeepAlive` 能覆盖崩溃场景，但覆盖不了「脚本被改坏导致起不来」——所以改完脚本务必先做健康检查。

---

## 附录 A 文件清单与职责对照

| 路径 | 性质 | 说明 |
|---|---|---|
| `~/.codex/config.toml` | 配置 | 提供商定义、默认模型与档位、目录路径、`[desktop]` 段 |
| `~/.codex/codex-model-router.py` | 程序（运行态） | launchd 实际执行的就是这份 |
| `~/.codex/custom-model-catalog.json` | 数据 | `/model` 面板内容。由脚本生成，**勿手写** |
| `~/.codex/regen-model-catalog.py` | 工具 | 重生成目录。每次升级 Codex 后运行 |
| `~/Library/LaunchAgents/<LABEL>.plist` | 系统任务 | 常驻与自拉 |
| `~/.zshrc`（或 `~/.bashrc`） | 凭证 | **唯一**存放真实密钥的地方 |
| `/tmp/codex-model-router.log` | 日志 | 路由、密钥状态、用量 |
| `~/.codex/sessions/**/rollout-*.jsonl` | 日志 | 每会话逐轮条目，排错第一手证据 |

若你把脚本同时放在一个代码仓库里做版本管理，**`~/.codex/` 下那份才是运行态**，改完记得双向同步，否则会出现「改了没生效」或「回滚丢改动」。

---

## 附录 B 非 macOS 平台

**小结：代理本身跨平台，常驻方式不跨平台。本手册只在 macOS 上验证过。**

代理是纯 Python 的 HTTP 服务，本身不依赖 macOS。需要替换的只有第 7.4 步的常驻机制：

| 平台 | 常驻机制 | 等价要点 |
|---|---|---|
| macOS | launchd LaunchAgent | 本手册正文 |
| Linux | systemd user unit | 用 `Restart=always` 对应 `KeepAlive`；同样要显式加载环境变量文件 |
| Windows | 计划任务或服务包装器 | 未验证；路径与环境变量加载方式差异较大 |

无论哪个平台，**「常驻进程拿不到交互式 shell 的环境变量」这个坑都存在**，形式不同而已。
